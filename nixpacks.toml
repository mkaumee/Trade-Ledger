[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd trader-ledger && npm ci"]

[phases.build]
cmds = ["cd trader-ledger && npm run build"]

[start]
cmd = "cd trader-ledger && npm start"
